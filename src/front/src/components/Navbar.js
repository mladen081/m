import './Navbar.css';

export const Navbar = () => {
    return (
        <div className="topnav">
            <a href="#">Home</a>
            <a href="#">Autors</a>
            <a href="#">Add Book</a>
        </div>
    );
}